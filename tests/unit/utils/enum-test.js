import enumAttr from 'dummy/utils/enum';
import { module, test } from 'qunit';

module('Unit | Utility | enum');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = enumAttr({ options: [] });
  assert.ok(result);
});
